# Rule engine config

Reusable **regex extractors** (dimensions, units, quantities) and **per-attribute matching policies**.

## Intended contents (when implemented)

- Dimension/unit patterns (e.g. `120x80 cm`, `5 mm`)
- Per-attribute matching rules and confidence thresholds
- Banned token lists, multi-entity guardrails

Schema TBD. Add YAML here when ready.
