# Global ontology config

**Product types (PT)** and **canonical attributes** per PT, plus **value domains** per attribute.

## Intended contents (when implemented)

- `product_types.yaml` (or per-PT files): e.g. `shower_tray`, `shower_enclosure`, `bathtub`
- Per-PT attribute definitions: e.g. `length_cm`, `width_cm`, `material`, `shape`, `installation`
- Value domains: enum lists + free-text rules per attribute

Schema TBD (see docs/EXTRACTION_FRAMEWORK.md). Add YAML here when ready.
