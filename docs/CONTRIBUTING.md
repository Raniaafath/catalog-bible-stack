# Contributing (evidence-based)

## What exists in this repo
- Dependency list in `requirements.txt`. (requirements.txt:L1-L7)
- Environment template in `.env.example`. (.env.example:L1-L109)
- Django management entrypoint in `manage.py`. (manage.py:L7-L18)
- Docker entrypoint in `entrypoint.sh`. (entrypoint.sh:L1-L11)
- Tests present in `catalog/tests.py` and `pub/tests/test_title_policies.py`. (catalog/tests.py:L1-L3, pub/tests/test_title_policies.py:L1-L91)

## Not found in repo
- Formatting/linting configuration (e.g., `pyproject.toml`). Not found in repo.
- CI configuration. Not found in repo.
- Contribution workflow or PR templates. Not found in repo.
